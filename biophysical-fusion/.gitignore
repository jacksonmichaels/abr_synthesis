__pycache__/
*.py[cod]
.ipynb_checkpoints/

# results/ and slurm_logs/ are no longer ignored from here: each has its own
# allowlist .gitignore so run write-ups are tracked while outputs are not.
# See ../.gitignore for the rationale.
